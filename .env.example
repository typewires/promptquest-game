# Optional: copy to .env (the game also supports pasting the key in the browser UI)
OPENAI_API_KEY=sk-...

# Optional model overrides (defaults are set in code)
# TEXT_MODEL=gpt-4o
# IMAGE_MODEL=gpt-image-1

