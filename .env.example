# Copy this file to .env and fill in values.

# ---- OpenAI ----
OPENAI_API_KEY=sk-...
# Optional: override model used for summaries
OPENAI_MODEL=gpt-4o-mini

# ---- Amadeus (Flights + optional delay/status if you add later) ----
# Create an Amadeus app and use your test credentials:
# https://developers.amadeus.com/
AMADEUS_HOST=test.api.amadeus.com
AMADEUS_CLIENT_ID=your_amadeus_client_id
AMADEUS_CLIENT_SECRET=your_amadeus_client_secret

# ---- App ----
# Cache TTL for external API calls (seconds)
CACHE_TTL_SECONDS=600
# Interval for watch mode (seconds)
WATCH_POLL_SECONDS=30
